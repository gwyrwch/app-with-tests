<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="test132132.OpenQEditPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Delete" Clicked="Delete_Clicked"/>
        <ToolbarItem Text="Save" Clicked="Save_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" VerticalOptions="Center" Padding="15">
            <Label 
                Text="Question: " 
                FontSize="18" 
            />
            <Entry 
                x:Name="QuestionText" 
                Placeholder="Your question here" 
                FontSize="Small"
            />
            <Label 
                Text="Answer:" 
                FontSize="18"
            />
            <Entry 
                x:Name="QuestionAnswer" 
                Placeholder="Your answer here" 
                FontSize="Small" 
            />
            <Label
                Text="Points:"
                FontSize="18"
            />
            <Entry
                x:Name="Points"
                Placeholder="Points for your question"
                x:DataType="x:Int32"
                FontSize="Medium"
                Keyboard="Numeric"
            />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
